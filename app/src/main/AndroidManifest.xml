<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.lixinwei.www.goldennews">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".app.GoldenNewsApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".newslist.NewsListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".likedlist.LikedListActivity"></activity>
        <activity android:name=".commentslist.CommentsActivity"
            android:label="Comments">
        </activity>
        <activity android:name=".newsDetail.NewsDetailActivity"
            android:theme="@style/DetailActivityTheme"></activity>
        <activity android:name=".DateNewsList.DateNewsListActivity"></activity>
    </application>

</manifest>